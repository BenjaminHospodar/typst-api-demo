# Placeholder only. Replace before applying to a real cluster.
# Do not put production secrets in git.
apiVersion: v1
kind: Secret
metadata:
  name: pdfgen-secrets
  namespace: pdfgen
type: Opaque
stringData:
  datasource-url: jdbc:postgresql://postgres:5432/pdfgen
  datasource-username: pdfgen
  datasource-password: changeme
  rabbitmq-username: pdfgen
  rabbitmq-password: changeme
  redis-password: changeme
  api-key: changeme-set-a-real-key
